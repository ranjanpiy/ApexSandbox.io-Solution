public String reverseWordsInASentence(String sentence){
    if(sentence == null)
    return null;
    List<String> sentenceList = new List <String>(sentence.split(' '));
    List<String> reverseList = new List<String>();
    for(Integer i=sentenceList.size() -1;i>=0;i--)
    {
        reverseList.add(sentenceList[i]);
    }
    String sentenceWordsReverse = String.join(reverseList,' ');
    return sentenceWordsReverse;
}