public Integer nextPrime(Integer num) {
    if( num<2)
     return 2;
    else if( math.mod(num,2)==0)
    { n= num+ 1;
        for( Integer i=2;i<n;i++)
        {
            if( math.mod(num,i)!=0)
            {return n;}
            goto n;
        }
    }
}