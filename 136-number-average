public Decimal average(List<Decimal> numbers){
    Integer size =numbers.size();
    Decimal Sum=0;
    for ( Decimal n:numbers)
    Sum = Sum + n;
    Decimal avg = (Sum/size).setScale(2);
  //  Decimal avgscale = avg.setScale(2);
    return avg;
}