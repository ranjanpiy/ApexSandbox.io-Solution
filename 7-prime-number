public Boolean isPrime(Integer num) {
    Boolean result;
    for ( Integer i=2; i<num; i++)
    {if(math.mod(num,i)==0)
       {result= false;
       break;}
       else{
           result=true;
       }
    }
    return result; 
}