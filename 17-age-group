public String ageGroup(Integer n) {
    String age;
    if(n<=1)
    { age= 'Infant';}
    else if ( 2<=n && n<=14)
    { age = 'Child';}
    else if ( 15<=n && <=21)
    {age = 'Youth';}
    else
    age = 'Adult';

    return age;
}